# UMK Kernel (Rust)

Production rewrite kernel for the Universal Mathematical Kernel.

```text
Kernel != Mathematics
Kernel + Laws = Mathematical System
```

The kernel knows nothing about mathematics. It represents expressions, matches patterns, applies first-class laws, checks types, records proof objects, and uses algebraic properties only when a Space declares them.

## Tests after vision alignment

```bash
cargo test --manifest-path kernel/rust/Cargo.toml
```

Current result: 16 passed, 0 failed.

## Instructions

Read `VISION_AND_USAGE.md` for the vision, invariants, and usage examples.
